import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'life-on-track',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
